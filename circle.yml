machine:
  services:
    - docker
  node:
    version: 6.5.0

test:
  override:
    - npm run testrpc:
        background: true
    - npm run lint
    - npm run unit-test -- --network=development
    - npm run integration-test -- --network=development
    - bash run_integration_tests.sh
